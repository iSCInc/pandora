apply from: rootProject.file('gradle/pandora_common_deps.gradle')
apply from: rootProject.file('gradle/publish_artifacts.gradle')

dependencies {
    compile project(':pandora-core')
    compile project(':pandora-service')
}
