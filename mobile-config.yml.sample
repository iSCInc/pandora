logging:
  level: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
    - type: file
      threshold: INFO
      currentLogFilename: /tmp/mobile-config.log
      archivedLogFilenamePattern: /tmp/mobile-config-%d.log.gz
      archivedFileCount: 5